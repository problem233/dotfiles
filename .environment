#!/bin/bash

export PATH=~/.local/bin:$PATH

if [[ $DISPLAY && ${DISPLAY-x} ]]; then
  export LANG="zh_CN.UTF-8"
else
  export LANG="en_US.UTF-8"
fi

export BASH_ENV=~/.environment
shopt -s expand_aliases

